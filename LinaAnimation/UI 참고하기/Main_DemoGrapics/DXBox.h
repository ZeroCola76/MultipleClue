#pragma once
/// 사용하지 않는 임시 파일
///구조가 이상하다. 물체가 렌더러 클래스를 가지는 것은 좀 많이 이상한것같다. -> 구조체임!
///디파인 헤더로 빼야하겠지만 복붙부터 해놓고 돌아가게해두자.
#include "NestRenderer.h"
#include <tuple>

//std::tuple<vector<Vertex>, vector<uint16_t>> MakeSquare();
//std::tuple<vector<Vertex>, vector<uint16_t>> MakeBox();

